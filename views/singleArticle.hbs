<div class="row">
  <div class="col s12 m6 l3">
    <div class="card large blue-grey">
      <div class="card-content white-text">
        <p>{{results}}</p>
        <span class="card-title activator">{{ article.title }}</span>
        <p class="activator">{{ article.preview }}</p>
        <br>
        <span class="tags">{{ article.keywords }}</span>
      </div>
      <div class="card-reveal">
        <span class="card-title">{{ article.title }}<i class="material-icons right">close</i></span>
        <p>{{ article.description }}</p>
      </div>
    </div>
<a class="waves-effect waves-light btn"><i class="material-icons left"></i>read</a>
<a class="waves-effect waves-light btn"><i class="material-icons right"></i>Add</a>
</div>
